package adresses.receptionofpeople
import adresses.People.ple

class Reception {
    var enumerate = mutableListOf<ple>()

    fun receptionOfPeople (name: String, adress: String): MutableList<ple> {
        return enumerate
}
}